import { BillingDirective } from './billing.directive';

describe('BillingDirective', () => {
  it('should create an instance', () => {
    const directive = new BillingDirective();
    expect(directive).toBeTruthy();
  });
});
