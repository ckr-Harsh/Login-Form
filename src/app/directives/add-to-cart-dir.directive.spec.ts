import { AddToCartDirDirective } from './add-to-cart-dir.directive';

describe('AddToCartDirDirective', () => {
  it('should create an instance', () => {
    const directive = new AddToCartDirDirective();
    expect(directive).toBeTruthy();
  });
});
