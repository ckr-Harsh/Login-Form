import { BillingCartDirective } from './billing-cart.directive';

describe('BillingCartDirective', () => {
  it('should create an instance', () => {
    const directive = new BillingCartDirective();
    expect(directive).toBeTruthy();
  });
});
