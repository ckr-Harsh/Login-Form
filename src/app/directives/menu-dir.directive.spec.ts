import { MenuDirDirective } from './menu-dir.directive';

describe('MenuDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MenuDirDirective();
    expect(directive).toBeTruthy();
  });
});
